export type Word = string;

class Words {
  private index: number | undefined = 0;
  private words: Word[] = []; 


  forward = ():Word => {
  
  }

  backword = (): Word=> {} 

};

export default Words